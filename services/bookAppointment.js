async function bookAppointment(pool,data){const c=await pool.getConnection();try{await c.beginTransaction();const [rows]=await c.execute("SELECT appointment_id FROM Appointments WHERE doctor_id=? AND appointment_date=? AND start_time=? AND status='booked' FOR UPDATE",[data.doctorId,data.appointmentDate,data.startTime]);if(rows.length){const e=new Error('SLOT_TAKEN');e.code='SLOT_TAKEN';throw e;}const [r]=await c.execute('INSERT INTO Appointments(patient_id,doctor_id,availability_id,appointment_date,start_time,end_time) VALUES(?,?,?,?,?,?)',[data.patientId,data.doctorId,data.availabilityId,data.appointmentDate,data.startTime,data.endTime]);await c.commit();return {...data,appointmentId:r.insertId,status:'booked'};}catch(e){await c.rollback();throw e;}finally{c.release();}} module.exports={bookAppointment};
