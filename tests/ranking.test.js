const {computeDoctorScore}=require('../services/rankDoctors');test('ranking score rewards availability, experience, rating and lower load',()=>{const best=computeDoctorScore({hoursUntilNextSlot:0,maxHoursUntilNextSlot:10,yearsExperience:10,maxYearsExperience:10,avgRating:5,bookedSlotsToday:0,dailyCapacity:8});const weaker=computeDoctorScore({hoursUntilNextSlot:10,maxHoursUntilNextSlot:10,yearsExperience:2,maxYearsExperience:10,avgRating:3,bookedSlotsToday:7,dailyCapacity:8});expect(best).toBeGreaterThan(weaker);});
